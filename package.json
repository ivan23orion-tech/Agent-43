{
  "name": "agent43",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "prisma generate && node -e \"if (process.env.POSTGRES_PRISMA_URL) { require('child_process').execSync('prisma db push', { stdio: 'inherit' }); } else { console.log('Skipping prisma db push: POSTGRES_PRISMA_URL not set'); }\" && next build",
    "start": "next start",
    "postinstall": "prisma generate",
    "prisma": "prisma",
    "cleanup:legacy-free-tasks": "node scripts/delete-legacy-free-tasks.js"
  },
  "dependencies": {
    "next": "14.0.4",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "@prisma/client": "^5.12.0"
  },
  "devDependencies": {
    "prisma": "^5.12.0"
  }
}

